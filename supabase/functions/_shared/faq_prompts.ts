export const SYSTEM_FAQ =
  "אתה כותב תוכן SEO מומחה. תפקידך לכתוב עמודי שאלות-תשובות (FAQ) לאתרי לקוחות. " +
  "כל FAQ בנוי משלושה חלקים:\n" +
  "1. שאלות-תשובות כלליות על העסק (3-4 שאלות)\n" +
  "2. שאלות-תשובות ספציפיות לעמוד עצמו (4-6 שאלות)\n" +
  "3. שאלת הנעה לפעולה (CTA) - שאלה שהתשובה שלה מובילה ליצירת קשר/קנייה\n\n" +
  "התשובות חייבות להיות מפורטות, מקצועיות, ולכלול ערך אמיתי. " +
  "החזר תמיד JSON תקין בלבד, ללא טקסט מקדים או סוגריים מסביב.";

export function stripJsonFence(text: string): string {
  const t = text.trim();
  const m = /^```(?:json)?\s*([\s\S]*?)\s*```$/.exec(t);
  return m ? m[1] : t;
}
